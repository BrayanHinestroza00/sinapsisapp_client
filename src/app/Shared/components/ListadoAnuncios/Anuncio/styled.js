import styled from "styled-components";

export const AnuncioTitulo = styled.h2`
  font-size: 2.5rem;
  text-align: center;
`;

export const AnuncioSpanHelper = styled.span`
  position: relative;
  top: 0.5rem;
  left: 0.5rem;
  font-size: 1.2rem;
  margin-right: 1rem;
`;

export const AnuncioParrafo = styled.p`
  font-size: 1.5rem;
  text-align: center;
`;
